#include "main.h"

/**
 * exec_ve - a
 * Return: b
 */

int exec_ve(void)
{
	return (0);
}



/*
 * execve(path, argv ,environ)
 * exemplo argv: argv[] = {"/bin/ls", "-l", NULL}
 * int val = execve(argv[0](<-path), argv, NULL)
 *
 * pid, ppid, fork, wait
 *
 * /

